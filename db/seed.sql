-- ============================================================================
-- 酒店记账 · 经营分析 · AI 定价系统  ——  初始化种子数据（最小生产版）
-- 入库前请先跑 schema.sql
--
-- 约定：只初始化「必须存在」的数据 —— 登录账号。其它一切为空，由系统自行产生：
--       · 全局配置     首次打开设置页自动创建（酒店名称/城市由用户自己填）
--       · 房间         首次导入 Excel 后按房号自动建档
--       · 渠道/费用项  首次导入 Excel 自动建档（新渠道佣金默认取全局配置）
--       · 档位价目     设置页由用户手工添加（定价建议的基准）
--       · 默认值       佣金率 0.10 / 每月营业天数 30.4 为代码内置，设置页可改
--       演示数据（房间 + 2026-08 流水）见 db/seed-demo.sql，需要时手动执行。
-- ============================================================================

SET NAMES utf8mb4;

USE hotel_accounting;

-- ---------------------------------------------------------------------------
-- 登录账号（预留单用户登录；后端用 BCrypt 校验）
--     用户名 admin / 默认密码 admin123（上线前务必修改！）
--     以下哈希由 bcrypt(gensalt rounds=10) 对 "admin123" 生成并已验证。
-- ---------------------------------------------------------------------------
INSERT INTO sys_user (username, password_hash, display_name) VALUES
  ('admin', '$2a$10$Fn4LGnPGHIVVdd1LF.BFj.am0DtStKb.Qt2uNjPi/vC9QsL99VMDa', '管理员');
