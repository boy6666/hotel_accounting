# 二期进度快照（断网/重开会话可从此恢复）

> 更新时间：2026-08-24 17:59｜上一动作：**二期开工**（docs/06/07/08 工单已扩写为完整规格，三条 agent 线已并行启动）

## 当前状态

- **契约已就绪（权威）**：`docs/03 §10 定价·预测` / `§11 回本测算` / `§14.3 /api/predict` / `§14.4 /api/llm/interpret`；`db/schema.sql` 二期表已建 DDL（pricing_suggestion / price_calc_scenario / prediction_result / breakeven_scenario / breakeven_cashflow）；seed 有档位（平日260/周末360/节假日520，节假日9/30–10/7）与 breakeven_scenario（捌宿·基准方案）。
- **工单已扩写**：`docs/06` FW-10/FW-11、`docs/07` BE-09/10（含建议价引擎系数表、降级、验收锚点）、`docs/08` SC-04/05（含天气降级、DeepSeek 红线/三态）。docs/05 总览已标二期开工 + 端口行更正（8081/8001/5174）。
- **docs/03 §11.5 已批注更正**：示例 58/2031-06 是旧占位；seed 参数实算 = 月供 12035.68、回本月 **151**、回本日 **2039-03**。
- **agent 线**：frontend-p2-2（FW-10/11）、backend-p2-2（BE-09/10）、sidecar-p2-2（SC-04/05）——本次为显式 model=sonnet 重拉起（默认 opus-4-8[1m] 不可用）。启动 ~15 分钟，**还在读契约、未落二期货代码**。
- 三端服务健康：sidecar 8001 ✓ / backend 8081 ✓ / frontend 5174 ✓。

## 关键拍板（二期默认值，可改）

1. **建议价引擎 v1 系数**（07 工单 §09-A）：档位基准价 × 预测入住率（≥90→1.06 / 85–89→1.03 / 75–84→1.00 / 60–74→0.97 / <60→0.94 / 无预测→1.00），round 到整元；手改 source='manual' 行不覆盖。
2. **天气降级**：旁车按 `hotel_config.city`（可空）调免费接口（wttr.in/open-meteo），失败/空 → 纯日历+节假日，不报错只记 degraded。
3. **DeepSeek**：key 走环境变量 DEEPSEEK_API_KEY（可 DEEPSEEK_BASE_URL/DEEPSEEK_MODEL），不落库；三态都 code=0（无key/failed→llmAvailable:false）；红线：出网只送聚合摘要。
4. **预测降级**：旁车挂/超时 → 主后端近3月加权×节假日系数兜底，engine=statistical、degraded=true，仍 200。

## 恢复步骤（断网后再开）

1. 从 `docs/05 §1` 的「启动提示词模板」+ 本快照，重起三条 agent 线（前端 FW-10/11、Java BE-09/10、旁车 SC-04/05）；**子 agent 一律显式 model=sonnet**（继承默认的 opus-4-8[1m] 不可用会死）。
2. 各线的验收锚点都在对应工单【二期·开工】节，对照 seed 逐项验收（建议沿用干净验收库方法论，见 07 工单尾部）。
3. 重开会话后重建每小时巡检（旧 cron 是会话级的，已随断网失效）；任务清单用 TaskList 恢复对应关系（11→前端 / 12→后端 / 13→旁车 / 14→二期联调验收）。
4. 清掉旧批次（上一个完成的 <date> 之前若有未收口的遗留口径，以 docs/03 为准修订而不是各家发挥）。
